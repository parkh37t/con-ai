# 화면설계 — 앰배서더 호텔

> RFP 확정 후 이 폴더에 2단 HTML 화면설계서를 생산한다(골든 복제→개조).

```
화면설계/
├── 프론트/   고객 채널 화면 (front-*.html)
├── 관리자/   운영 백오피스 화면 (admin-*.html)
└── _공통/    프로젝트 DS·골든 템플릿 (RFP 확정 후 등록)
```

- 생산 절차: `docs/02_SSOT/*_작업가이드.md` + `.claude/skills/screen-spec/SKILL.md`
- 입력: `docs/00_입력원본/` RFP·요구사항서 → 요건 추출 → 화면 매핑
- 🟡 현재 화면 0건 — RFP 투입 후 우선순위 화면부터 생산.
