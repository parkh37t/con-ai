# 미확정 목록

사람이 결정해야 하는 항목이다. 결정 전에는 합성 데이터와 fixture 로 개발을 계속하고, 미확정 원본을 승인된 사실로 바꾸지 않는다(설계 §15, 개발프롬프트).

| 번호 | 항목 | 왜 필요한가 | 결정 전 진행 방법 | 출처 |
|---|---|---|---|---|
| U-001 | REQ-SFR-066-001 의 정본 — 원장 행 308 "관심 물품·공급업체 관리 기능 제공" vs HTML 설명 "몰 통합 관리" | 매핑 승인·커버리지 계산의 기준 | `fixtures/conflicts/req-sfr-066-001.conflict.json` 을 conflict 상태로 보존, 승인 불가 | 설계 §7, 보고서 §4 |
| U-002 | INDEX 중복 ID 43그룹(서로 다른 경로 32그룹)의 실제 화면 구분 | 외부 화면 ID 유일성 | `planImport` 가 임시 레코드로 분리, 공식 레지스트리에 합치지 않음 | 보고서 §3, `packages/domain` |
| U-003 | 경로 미확인 52건의 이동·폐기 여부(43건은 같은 파일명 후보 있음) | 화면 목록 수입 | `path_resolution_required` 로 보류 | 보고서 §3 |
| U-004 | 상태 모델 3계열(더미 규칙·개정 용어집·매크로 상태)의 적용 범위와 채택 버전 | StateModel 기준정보 | 원천별 별도 레코드로 저장, 합치지 않음 | 보고서 §4 |
| U-005 | 승인할 golden(참고 화면) 버전 | 참조 복제의 기준 | 샘플 프로젝트의 레퍼런스 3개는 합성 예시로만 사용 | 설계 §15 |
| U-006 | 검토 데이터(요구사항 원문·INDEX·추적후보·인벤토리)를 공개 저장소에 커밋할지 | 다음 세션에서 무결성 테스트·수입 비교 실행 | `.gitignore` 로 제외, 로컬 배치(`data/review/README.md`) | 사용자 결정 대기 |
| U-007 | 라이브러리·모델 버전의 공식 문서 확인 (이 환경은 문서 사이트 접근 차단) | 버전 고정의 근거 | npm dist-tags 로만 확인한 상태로 고정(TypeScript 5.9.3, vitest 4.1.11, zod 4.5.4, hono 4.13.7, react 19.2.8, vite 8.2.2, @anthropic-ai/sdk 0.124.0, playwright 1.63.0) | 개발프롬프트 |
| U-008 | 파일럿 대표 화면 10~20개와 검증할 업무 조건 | 설계 §14 단계 0 | 샘플 프로젝트 화면 3개로 세로 조각 검증 | 설계 §14 |
| U-009 | 실제 모델 호출 검증 — 이 환경에 API 키·토큰이 없어 anthropic 어댑터는 가짜 fetch 로만 테스트함 | 구조화 출력이 실제 API 에서 그대로 받아들여지는지 | `.env` 에 키 또는 토큰을 넣고 `MODEL_ADAPTER=anthropic` 으로 생성 1회 실행해 확인 | 어댑터 보고 |
| U-010 | AS-IS 분석(URL·앱 → 페인포인트)의 입력 범위와 산출 형식 | 4단계 프로세스 ① | 세로 조각 이후 2단계로 계획 | 사용자 요구 |
| U-011 | 승인 이후 재생성 시 버전 규칙(v1.1? stale 표시?)과 v1.0 폴더 보존 정책 | 완료·이관 프로세스 | 화면당 v1.0 한 번, 재생성 시 화면 상태만 review 로 복귀 | 설계 §11 |
| U-012 | 디자인 에이전트가 받을 이관 manifest 의 확정 필드 | 4단계 프로세스 ④ | manifest.design_handoff 초안(screen_revision_id, design_input_spec_hash, locked_elements/actions, allowed_tokens) | 설계 §12 |
| U-013 | REQ 출처(회의·RFP·페인포인트) 분류 체계 — 산출물은 「출처 링크 필수」라고 하는데 저장소에 필드가 없고 `SourceType` 에도 종류 구분이 없다 | RTM 표의 출처 열, G0 킥오프의 「REQ 100% 출처 보유」 | 필드를 만들지 않고 RTM 표에 «출처 미기록» 을 명시. 합성 시드에 출처를 지어내면 거짓 데이터가 된다 | 산출물 ID 표, P1-05 |
| U-014 | 요소 태깅의 정본을 언제 ScreenSpec 으로 옮길 것인가 | 화면 재생성 시 태그 고아 문제 | `FunctionEntry.element_refs`(FN→요소)로 두고 stale 을 보고만 한다. 결정문서 0015 결정 2 | 결정문서 0015 |
| U-015 | IA 를 재배치했을 때의 번호 정책 — 위치 불일치를 방치할지, 사람이 개명할지, 계층에서 뗀 일련번호로 갈지 | 계층 코드 ID 의 알려진 대가 | 자동 개명하지 않고 발번 응답의 `differs` 로 알리기만 한다 | 결정문서 0015 결정 5 |
| U-016 | 비UI 전용 REQ 가 있을 때 G1 추적성을 충족으로 볼 것인가 | G1 게이트 정의 | 보수적으로 통과로 보지 않고 이유를 적는다. 「화면 밖 처리」 승인 수단이 없기 때문 | P1-05, G1 |
| U-017 | SCR-2.3.1 표기를 도입할 것인가 (기존 `SAMPLE-quote-list`·`SCREEN-001` 과 병기) | 산출물 ID 형식 일치 vs 두 ID 를 오가는 혼란 | 도입하지 않고 기존 외부 ID 를 그대로 RTM 표에 적는다 | 산출물 ID 표 |
| U-018 | 시드의 IA↔REQ 배치(목록←REQ-QT-001·004, 상세←REQ-QT-002)는 골든 명세를 참고한 개발용 합성 값이다 | 첫 화면이 갭 제안을 보여 주기 위한 배치 | 실제 업무 매핑이 아님을 진행기록에 명시 | 기획자 결정 2026-09-06 |
